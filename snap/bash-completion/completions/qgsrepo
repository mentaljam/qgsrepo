_qgsrepo()
{
    local cur prev words cword
    _init_completion -s || return

    if [[ $cur == -* ]]; then
        COMPREPLY=($(compgen -W '$(_parse_help "$1")' -- "$cur"))
    else
        _filedir
    fi
} &&
complete -F _qgsrepo qgsrepo
